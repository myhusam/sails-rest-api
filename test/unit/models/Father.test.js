import { assert } from 'chai';
import Model from '../../../api/models/Father';
import Fixture from '../../fixtures/FatherFixture';

describe('models:Father', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
