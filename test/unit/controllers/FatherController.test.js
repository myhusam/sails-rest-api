import { assert } from 'chai';
import Controller from '../../../api/controllers/FatherController';

describe('controllers:FatherController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
